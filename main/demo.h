// Dedicated Bloub page interface. Other demo sources remain as reference material.
#pragma once

#include "bsp_button.h"

void demo_bloub_enter(void); void demo_bloub_exit(void);
void demo_bloub_key(bsp_btn_t btn, bsp_btn_ev_t ev);
